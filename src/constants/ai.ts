/**
 * Safety Gate: message revision remains unavailable until the server-side
 * ChatCommand guard and durable revision semantics are implemented.
 */
export const AI_MESSAGE_REVISION_ACTIONS_ENABLED = false;
